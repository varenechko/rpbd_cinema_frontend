import styled from "@emotion/styled";
import { Tabs } from "@mui/material";

export const TabsStyled = styled(TabsList)({
    // borderBottom: '1px solid #e8e8e8',
    '& .MuiTabs-indicator': {
      backgroundColor: '#000',
    },
  });